package viaCep;
/*
import org.junit.jupiter.api.Test;
import static io.restassured.RestAssured.given;


public class RestAssuredTest {
  String url = "https://reqres.in/api/users";

  @Test
  public void getPageOne(){
	  given().param("page", "1").when().get(url).then().statusCode(200).body("page", equalsTo(1));
	  

      given().
              param("page", "1").
              when().
              get(url).
              then().
              statusCode(200).
              body("page", equalsTo(1));
  }

  @Test
  public void getUser() {
      get(url + "/2").then().body("data.id", equalsTo(2));
  }


}
*/
